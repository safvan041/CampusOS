"""
API router for automatic route registration.
"""
from rest_framework.routers import DefaultRouter

router = DefaultRouter()

# Register viewsets here
# Example: router.register('users', UserViewSet)

urlpatterns = router.urls
