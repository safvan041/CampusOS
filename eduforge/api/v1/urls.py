"""
API v1 URL configuration.
"""
from django.urls import path
from . import views

app_name = 'v1'

urlpatterns = [
    # Add your v1 API endpoints here
    # Example: path('users/', views.UserListView.as_view(), name='user-list'),
]
